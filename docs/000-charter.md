# 000 Charter

Pete is the organism, not any single subsystem. `pete` is simulator-first, real-robot second, and treats every hard-coded behavior as a seed that can later be shadow-trained, compared, promoted, or safely replaced.
