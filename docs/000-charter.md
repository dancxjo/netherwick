# 000 Charter

Pete Netherwick is the organism, not any single subsystem. `netherwick` is simulator-first, real-robot second, and treats every hard-coded behavior as a seed that can later be shadow-trained, compared, promoted, or safely replaced.
