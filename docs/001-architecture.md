# 001 Architecture

The main flow is sensors → `Now` → Sensations → teacher vectors → `ExperienceInstant` → latent encoding → imagined futures → conductor choice → autonomic safety → body actuation → next `Now` → reward/surprise → ledger → training.

The canonical moment representation is [ExperienceInstant](instant.md), not a separate Sensorium layer.
